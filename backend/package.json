{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "scripts": {
    "test": "tsc && cd build && node main.js && cd .."
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
